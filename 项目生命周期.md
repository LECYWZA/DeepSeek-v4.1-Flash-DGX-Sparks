# DeepSeek-V4.1-Flash · 四机环网部署（Mia/SGLang）· 项目生命周期

> 本文件是本项目的状态唯一入口：任何改动/调研/决策请**追加记录**（保时序，不覆盖历史）。
> 下次打开仓库，先读本文件即可了解项目当前情况；维护动作请顺手记录并提交 GitLab。

## 项目目标

把 **DeepSeek-V4.1-Flash（SGLang，四机 TP4/EP4 无交换机环网）** 稳定服务在本队 4×DGX Spark 上：

- 1M 上下文、KV 池 pin 4.3M tokens、abliterated（uncensored）变体；
- 大上下文 prefill 尽量快（Mia 栈实测 ~2.1–2.4k tok/s，优于 vLLM/TR3 栈）；
- **KV 语义（用户要求）**：KV 够用时**永不驱逐**缓存；仅当新请求空间不足时按 **LRU 优先驱逐最久未聊的会话**；1M 压缩会话属正常失效；
- 端点约定：**`8888` = 薄代理（注入 session_id、任意模型名归一化）；`8000` = 引擎本体（裸口）**；
- 运维：模型跑起来后不由助手停止（用户自行管理）；API key 保持原样。

## 机器与网络（详见 `C:\Users\Administrator\.config\opencode\机器环境.md`）

- spark-1 `.153`（head, rank0）｜spark-2 `.118`｜spark-3 `.128`｜spark-4 `.196`（WiFi 管理网）
- 环网：S1–S2(100)｜S2–S3(102)｜S3–S4(103)｜S4–S1(101)，MTU9000；控制面走 K2P 段 `192.168.123.x`
- 权重：`$HOME/NewModels/DeepSeek-V4.1-Flash[-Abliterated]`；Engram 本地 packed：`$HOME/dsv41-engram`
- NFS：head 导出权重，worker 经环网挂载（`NFS_SERVER_IPS`）；`share` 必须先于 `serve`（冷启动顺序）

## 仓库与远端约定

- `origin` = 本队 GitLab `http://192.168.1.1:180/root/DeepSeek-v4.1-Flash-DGX-Sparks.git`（**提交/推送都在这里**）
- `github` = `git@github.com:LECYWZA/DeepSeek-v4.1-Flash-DGX-Sparks.git`（fork，备用）
- 上游 = MiaAI-Lab/DeepSeek-v4.1-Flash-DGX-Sparks（同步只读）
- 机器本地文件（.gitignore，不入库）：`.env.tp4`、`state-tp4/`、`logs-tp4/`

## 运维命令

```bash
cd /models/DeepSeek-v4.1-Flash-DGX-Sparks
./dspark.sh                 # 控制台（TP4 默认）
./start-tp4.sh doctor|build|share|pack|serve|stop|status|logs|smoke
# 全停后重启顺序：share → serve（或 dspark 菜单）
```

- 端点：`http://192.168.1.153:8888/v1`（代理，客户端用）；`http://192.168.1.153:8000/v1`（引擎裸口，调试/对照）
- 代理日志：`state-tp4/session-proxy.jsonl`（容器内 `/state` = 宿主 `state-tp4/`）
- 引擎日志：`logs-tp4/dsv41.log`

## 当前冻结配置（`.env.tp4`，2026-09-20）

| 键 | 值 | 说明 |
|---|---|---|
| `PORT` | **8000** | 引擎端口（8888 让给代理） |
| `SESSION_PROXY_PORT` | **8888** | 代理端口（0/空 = 关闭代理） |
| `SESSION_PROXY_UPSTREAM` | `http://127.0.0.1:8000` | 代理上游 |
| `MAX_TOTAL_TOKENS` | **4300000** | KV 池 pin（4.3M） |
| `CHUNKED_PREFILL_SIZE` | **2560** | 预填充块（=10×256 页对齐） |
| `MAX_RUNNING_REQUESTS` | **6** | 并发 |
| `DSPARK_BLOCK_SIZE` | **5** | DSpark k=5（#20/#21：结构化/代码更优） |
| `EXTRA_SGLANG_ARGS` | 原三项 + `--enable-metrics --enable-metrics-for-all-schedulers --enable-session-radix-cache` | metrics 全 rank 上报；会话感知缓存 |
| `DSV41_INDEXER_SCORE_BUDGET_MIB` | 256 | adapter 钩子：layer-20 候选拷贝 1GiB→256MiB（c 14B→11B，预算 +30%） |
| 其余 | `CONTEXT_LENGTH=1048576`、`MEM_FRACTION_STATIC/HEAD=0.80`、`expandable_segments:False`、ablit 变体 | 不变 |

## 本分支新增/修改（vs 上游，2026-09-20）

- **新增** `scripts/session_proxy.py`：薄代理（stdlib）；① 会话指纹/`X-Session-Id` → 注入 `session_id`（顶层+extra_body）；② `model` 任意名 → `SERVED_MODEL_NAME` 归一化；③ 全透传（SSE/health/models/abort_request/auth）；④ JSONL 审计日志。
- **新增** `adapter/indexer_budget.py` + `sitecustomize.py` 接线：索引器候选预算 1GiB→256MiB。
- **修改** `boot.py`：rank0 在引擎旁启动代理（进程组随 stop 一起收），Ready 行打印代理端口。
- **修改** `start.sh`：head/worker 透传 `DSV41_INDEXER_SCORE_BUDGET_MIB`；head 另透传 `SESSION_PROXY_*`。
- **修改** `Dockerfile`：`EXPOSE 8000 8888`。
- **新增** 本文件。

## 已知问题 / 结论（社区调研）

- SGLang **无前缀 TTL**：仅按需驱逐；"闲置变冷"来自：池不足时的 LRU 驱逐 / 混合-SWA 驱逐类 bug / DSpark 超调污染缓存键（sglang#35694，未合并）。
- 会话感知驱逐=pull #29173（已合入）：**先驱逐无引用，再 LRU 驱逐被引用**（老会话先走）。
- SPS 表 + Engram 不可用（上游 #39173 未修）→ 保持关闭；并发提速用"加槽位"（本机按用户定 6）。
- NFS share/serve 顺序、head 内存/clamp、`expandable_segments` 禁用等坑见 `docs/chunked-prefill-memory.md` 与 `docs/switchless-ring.md`。

## 记录（追加）

- **2026-09-20**：落地"代理 8888 + 引擎 8000"分离、session-cache、metrics、indexer 预算 256M、4.3M/2560/MRR6/k5；`#23` 内存碎片 compact systemd 单元（四台）。
- **待补**：起服自检结果（KV 池实际值/端点/任意模型名/session_id 注入/日志落盘）与用户 500K 双会话实测结论。

## 2026-09-21 · 配置变更（3072 / 去代理 / 引擎 8888 / 时区 CST）

- **变更**（`.env.tp4`，备份 `.env.tp4.bak-20260921-011640`）：
  - `PORT` 8000 → **8888**（引擎直连；客户端/监控面板零改动）
  - `SESSION_PROXY_PORT` 8888 → **0**（**停用薄代理**；不再注入 session_id；失去 JSONL 审计）
  - `CHUNKED_PREFILL_SIZE` 2560 → **3072**
  - `MAX_RUNNING_REQUESTS` 6 → **4**（SWA 池按 4 并发重算为 17,408）
  - `EXTRA_SGLANG_ARGS` 移除 `--enable-session-radix-cache`
- **start.sh**：head/worker 容器增加 `-e TZ=Asia/Shanghai`（日志/时间戳转北京时间）；已提交 GitLab `9f1d625`。
- **重启**：`stop` → `serve` 正常；`Ready: API on port 8888`；容器 healthy；`max_num_reqs=4`、`chunked_prefill_size=3072`、`port=8888` 已核对。
- **980K 复测**（t980.py，989,801 tokens 全新内容 + 续聊）：
  - Turn1（全新 prefill）：**732 s**（2560 时 770.5 s，+5%）；全程无卡顿、无 OOM。
  - Turn2（续聊）：**8.1 s**，`cached_tokens=989,696`（**99.99% 命中**）。
  - **瞬态谷值（MemAvailable）**：**6.19 GiB**（2560 时 6.63 GiB；危险线 4-5 GB）→ **3072 保留**。
- 备注：缓存 miss 的根因是 opencode 客户端每轮组装差异（动态 system/工具输出裁剪/历史管理），非服务端缓存问题；内核实测（同内容续聊）99.99% 命中。
